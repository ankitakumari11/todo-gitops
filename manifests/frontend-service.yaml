apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: todo-app
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
  # ClusterIP — Ingress will route external traffic to this service
  # You don't expose frontend directly via NodePort